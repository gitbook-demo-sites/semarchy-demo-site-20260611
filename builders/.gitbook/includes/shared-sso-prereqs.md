### Shared SSO prerequisites

- confirm the target identity provider and assertion flow
- confirm which deployment track is being configured
- confirm who owns the rollback path if login fails
- confirm where support escalation routes after cutover
